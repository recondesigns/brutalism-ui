import { Meta, Title, Subtitle, ArgTypes, Canvas } from "@storybook/blocks"
import * as CardActionsStories from "./CardActions.stories"
import * as CardStories from "./Card.stories"

<Meta of={CardActionsStories} />

<Title>CardActions</Title>

<Subtitle>Provides a container for the call to action buttons.</Subtitle>

<Canvas of={CardActionsStories.Demo} />

## Overview

[Github](https://github.com/recondesigns/brutalism-ui/tree/main/src/components/Card/CardActions.tsx)

Adding content with consistent padding can be done easily by using the Building block components.

## Basic usage

Import the component and nest the content in the CardContent component.

```js
import { Card, CardActions, Button } from "ubiq-ui"
</Card>
```

<Canvas of={CardActionsStories.Default} sourceState="shown" />
